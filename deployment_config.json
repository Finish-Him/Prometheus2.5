
{
  "definition": "Parâmetros de produção: endpoint, hardware, serialização.",
  "fields": {
    "model_path": "string",
    "serving_framework": "string",
    "resources": {
      "CPU": "string",
      "GPU": "string"
    }
  }
}

